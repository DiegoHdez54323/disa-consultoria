---
import { Sparkles, ArrowRight } from "lucide-astro";
---

<section class="relative py-32">
  <div class="container mx-auto px-6">
    <div class="relative max-w-4xl mx-auto scroll-reveal">
      {/* Background glow */}
      <div
        class="absolute inset-0 bg-linear-to-r from-primary/20 via-accent/20 to-secondary/20 rounded-3xl blur-3xl"
      >
      </div>

      {/* Card */}
      <div
        class="relative bg-card/50 backdrop-blur-xl border border-border/50 rounded-3xl p-12 md:p-16 text-center overflow-hidden"
      >
        {/* Decorative elements */}
        <div
          class="absolute top-0 right-0 w-64 h-64 bg-linear-to-br from-primary/10 to-transparent rounded-full blur-2xl"
        >
        </div>
        <div
          class="absolute bottom-0 left-0 w-48 h-48 bg-linear-to-tr from-secondary/10 to-transparent rounded-full blur-2xl"
        >
        </div>

        <div class="relative z-10">
          <span
            class="inline-flex items-center gap-2 px-4 py-1 rounded-full bg-primary/10 text-primary text-sm font-inter font-medium mb-6"
          >
            <Sparkles class="w-4 h-4" />
            Comencemos
          </span>
          <h2
            class="font-orbitron text-3xl md:text-5xl font-bold text-foreground mb-6"
          >
            ¿Listo para <span class="text-gradient-primary">transformar</span> tu
            negocio?
          </h2>
          <p
            class="font-inter text-lg text-muted-foreground mb-10 max-w-2xl mx-auto"
          >
            Cuéntanos tu idea y exploremos juntos cómo podemos convertirla en una
            solución digital de clase mundial.
          </p>
          <a
            href="/contacto"
            class="inline-flex items-center gap-3 px-10 py-5 rounded-full bg-gradient-primary text-primary-foreground font-inter font-semibold text-lg hover:glow-primary-lg transition-all group"
          >
            Iniciar conversación
            <ArrowRight
              class="w-5 h-5 group-hover:translate-x-1 transition-transform"
            />
          </a>
        </div>
      </div>
    </div>
  </div>
</section>