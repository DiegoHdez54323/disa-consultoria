---
import { softwareCategories } from "@/data/nosotros/softwareCategories";
---

<section class="py-24 relative">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16 animate-slide-up">
      <span class="text-primary font-medium text-sm tracking-wider uppercase mb-4 block">
        Nuestras especialidades
      </span>
      <h2 class="font-sora text-3xl md:text-4xl font-bold mb-4">
        ¿Qué <span class="text-gradient-primary">hacemos</span>?
      </h2>
      <p class="text-muted-foreground max-w-2xl mx-auto">
        Desarrollamos software a medida en estas categorías principales.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
      {softwareCategories.map((category, index) => (
        <div
          class="group relative p-6 rounded-2xl bg-card/50 border border-border/50 hover:border-primary/30 transition-all duration-300 animate-fade-in"
          style={`animation-delay: ${index * 0.1}s;`}
        >
          <div class="absolute inset-0 bg-gradient-to-br from-primary/5 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity" />
          <div class="relative">
            <div class="w-12 h-12 rounded-xl bg-primary/10 flex items-center justify-center mb-4 group-hover:bg-primary/20 transition-colors">
              <category.icon className="w-6 h-6 text-primary" />
            </div>
            <h3 class="font-sora text-lg font-semibold mb-2 text-foreground">
              {category.title}
            </h3>
            <p class="text-muted-foreground text-sm">{category.description}</p>
          </div>
        </div>
      ))}
    </div>

    <p class="text-center text-muted-foreground text-sm animate-fade-in">
      Los detalles de paquetes y precios están en{" "}
      <a href="/servicios" class="text-primary hover:underline">
        Servicios →
      </a>
    </p>
  </div>
</section>
