---
import { Mail, Phone } from "lucide-astro";
---

<section class="py-24 relative overflow-hidden section-fade-top">
  <div
    class="absolute inset-0 bg-linear-to-b from-transparent via-primary/5 to-transparent"
  >
  </div>

  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] rounded-full bg-primary/10 blur-3xl animate-pulse"
  >
  </div>

  <div class="container mx-auto px-6 relative">
    <div class="max-w-3xl mx-auto text-center animate-slide-up">
      <h2 class="font-sora text-3xl md:text-4xl lg:text-5xl font-bold mb-6">
        Convirtamos tu idea en un sistema que{" "}
        <span class="text-gradient-primary">sí se entrega</span>
      </h2>
      <p class="text-muted-foreground mb-8 text-lg">
        Proceso claro, entregables definidos y comunicación constante.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/contacto?tipo=llamada"
          class="inline-flex items-center justify-center gap-2 rounded-md bg-gradient-primary hover:opacity-90 text-primary-foreground font-semibold px-8 py-3"
        >
          <Phone className="w-4 h-4" />
          Agendar llamada
        </a>
        <a
          href="/contacto"
          class="inline-flex items-center justify-center gap-2 rounded-md border border-primary/30 hover:bg-primary/10 px-8 py-3 font-semibold"
        >
          <Mail className="w-4 h-4" />
          Contactar
        </a>
      </div>

      <p class="mt-8 text-sm text-muted-foreground animate-fade-in">
        Primera llamada sin costo • Sin compromiso • Respuesta en 24h
      </p>
    </div>
  </div>
</section>
