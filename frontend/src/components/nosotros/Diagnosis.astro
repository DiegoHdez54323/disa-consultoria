---
import { Sparkles } from "lucide-react";
import { diagnosisFeatures } from "@/data/nosotros/diagnosisFeatures";
import DisaFullLogo from "@/assets/DisaFullLogo.svg";
---

<section class="py-12 relative overflow-hidden">
  <div
    class="absolute inset-0 bg-linear-to-b from-primary/5 via-transparent to-transparent"
  >
  </div>

  <div class="container mx-auto px-6 relative">
    <div class="text-center mb-16 animate-slide-up">
      <span
        class="inline-flex items-center gap-2 text-primary font-medium text-sm tracking-wider uppercase mb-4 px-4 py-2 rounded-full bg-primary/10 border border-primary/30"
      >
        <Sparkles className="w-4 h-4" />
        Fase 0
      </span>
      <h2 class="font-sora text-3xl md:text-4xl font-bold mb-4 block">
        <span class="inline-flex items-baseline gap-3">
          Diagnóstico
          <span class="text-gradient-primary inline-flex items-baseline">
            <DisaFullLogo
              class="h-[0.95em] md:h-[1.0em] w-auto inline-block translate-y-1"
            />
          </span>
        </span>
      </h2>
      <p class="text-muted-foreground max-w-2xl mx-auto">
        La base de un proyecto exitoso: claridad total antes de construir.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="space-y-4">
        {
          diagnosisFeatures.map((feature, index) => (
            <div
              class="flex gap-4 p-4 rounded-xl bg-card/50 border border-border/50 hover:border-primary/30 transition-all animate-fade-in"
              style={`animation-delay: ${index * 0.1}s;`}
            >
              <div class="w-10 h-10 rounded-lg bg-primary/10 flex items-center justify-center shrink-0">
                <feature.icon className="w-5 h-5 text-primary" />
              </div>
              <div>
                <h4 class="font-semibold text-foreground mb-1">
                  {feature.title}
                </h4>
                <p class="text-sm text-muted-foreground">
                  {feature.description}
                </p>
              </div>
            </div>
          ))
        }

        <div
          class="p-4 rounded-xl bg-secondary/10 border border-secondary/30 animate-fade-in"
        >
          <p class="text-sm text-muted-foreground">
            <strong class="text-secondary">Nota:</strong> El prototipo es conceptual,
            para validar experiencia y alcance. No es el sistema final ni incluye
            backend real.
          </p>
        </div>
      </div>

      <div class="relative animate-scale-in">
        <div
          class="relative rounded-2xl border border-border/50 bg-card/50 overflow-hidden p-4"
        >
          <div
            class="flex items-center gap-2 mb-4 pb-4 border-b border-border/50"
          >
            <div class="flex gap-1.5">
              <div class="w-3 h-3 rounded-full bg-destructive/50"></div>
              <div class="w-3 h-3 rounded-full bg-yellow-500/50"></div>
              <div class="w-3 h-3 rounded-full bg-green-500/50"></div>
            </div>
            <div class="flex-1 mx-4">
              <div class="h-6 rounded-full bg-muted/50 max-w-xs"></div>
            </div>
          </div>

          <div class="grid grid-cols-2 gap-3">
            <div
              class="aspect-video rounded-lg bg-muted/30 p-3 border border-border/30"
            >
              <div class="h-2 w-16 bg-primary/30 rounded mb-2"></div>
              <div class="grid grid-cols-2 gap-2">
                <div class="h-8 bg-primary/10 rounded"></div>
                <div class="h-8 bg-secondary/10 rounded"></div>
              </div>
              <div class="h-12 bg-muted/50 rounded mt-2"></div>
            </div>

            <div
              class="aspect-video rounded-lg bg-muted/30 p-3 border border-border/30"
            >
              <div class="h-2 w-12 bg-primary/30 rounded mb-2"></div>
              <div class="space-y-1.5">
                <div class="h-3 bg-muted/50 rounded"></div>
                <div class="h-3 bg-muted/50 rounded w-3/4"></div>
                <div class="h-3 bg-muted/50 rounded w-1/2"></div>
              </div>
            </div>

            <div
              class="aspect-video rounded-lg bg-muted/30 p-3 border border-border/30"
            >
              <div class="flex gap-2 mb-2">
                <div class="w-8 h-8 bg-primary/20 rounded"></div>
                <div class="flex-1">
                  <div class="h-2 bg-muted/50 rounded w-1/2 mb-1"></div>
                  <div class="h-2 bg-muted/50 rounded w-3/4"></div>
                </div>
              </div>
              <div class="h-8 bg-muted/50 rounded"></div>
            </div>

            <div
              class="aspect-video rounded-lg bg-muted/30 p-3 border border-border/30"
            >
              <div class="h-2 w-10 bg-primary/30 rounded mb-2"></div>
              <div class="space-y-1.5">
                <div class="h-4 bg-muted/50 rounded"></div>
                <div class="h-4 bg-muted/50 rounded"></div>
                <div class="h-4 bg-primary/30 rounded w-1/3"></div>
              </div>
            </div>
          </div>

          <div class="mt-4 text-center">
            <span class="text-xs text-muted-foreground">
              Demo Conceptual • 3-5 pantallas navegables
            </span>
          </div>
        </div>

        <div
          class="absolute -top-4 -right-4 w-24 h-24 bg-primary/20 rounded-full blur-2xl animate-pulse"
        >
        </div>
        <div
          class="absolute -bottom-4 -left-4 w-32 h-32 bg-secondary/20 rounded-full blur-2xl animate-pulse"
        >
        </div>
      </div>
    </div>
  </div>
</section>
