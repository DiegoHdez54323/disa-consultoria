---
// Importamos el mismo componente React que usamos en la página de contacto
import { ContactForm } from "../../components/contact/ContactForm";
---

<section class="py-24 relative overflow-hidden section-fade-top" id="contacto">
  {
    /* Elementos de fondo decorativos (opcional, para mantener el estilo del index) */
  }
  <div class="absolute inset-0 bg-grid opacity-20 pointer-events-none"></div>
  <div
    class="absolute top-0 left-1/2 -translate-x-1/2 w-full max-w-[1000px] h-[500px] bg-primary/5 rounded-full blur-3xl pointer-events-none"
  >
  </div>

  <div class="container mx-auto px-6 relative z-10">
    <div class="max-w-3xl mx-auto">
      {/* Encabezado de la sección */}
      <div class="text-center mb-16">
        <h2
          class="font-sora text-4xl md:text-5xl font-bold text-foreground mb-6"
        >
          Hablemos de tu <span class="text-gradient-primary"
            >Siguiente Idea</span
          >
        </h2>
        <p class="font-inter text-xl text-muted-foreground">
          Ya sea que tengas un proyecto definido o solo una idea vaga, estamos
          aquí para ayudarte a concretarla.
        </p>
      </div>

      <div class="mx-auto">
        <ContactForm client:visible />
      </div>
    </div>
  </div>
</section>
