---
import "../styles/global.css";
const now = new Date().toLocaleString("es-MX");
---

<main class="min-h-screen bg-background text-foreground px-4 py-10">
  <div class="mx-auto flex max-w-5xl flex-col gap-10">
    {/* TÍTULO */}
    <section class="space-y-2">
      <h1 class="text-3xl font-orbitron">Playground de estilos DiSa</h1>
      <p class="text-sm text-muted-foreground">
        Fecha/hora: {now}
      </p>
      <p class="text-xs text-muted-foreground">
        Si ves bien los colores de fondo, textos y las tarjetas, Tailwind 4 +
        globals.css están funcionando.
      </p>
    </section>

    {/* COLORES DE TEXTO */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">Colores de texto</h2>
      <div class="grid gap-2 text-sm md:grid-cols-2">
        <p class="text-primary">.text-primary → este es text-primary</p>
        <p class="text-secondary">.text-secondary → este es text-secondary</p>
        <p class="text-accent">.text-accent → este es text-accent</p>
        <p class="text-muted-foreground">
          .text-muted-foreground → este es text-muted-foreground
        </p>
        <p class="text-destructive">
          .text-destructive → este es text-destructive
        </p>
        <p class="text-foreground">
          .text-foreground → color de texto principal
        </p>
      </div>
    </section>

    {/* BACKGROUNDS / CARDS */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">Fondos y tarjetas</h2>
      <div class="grid gap-4 text-sm md:grid-cols-3">
        <div class="rounded-xl bg-primary p-4 text-primary-foreground">
          <p>.bg-primary + .text-primary-foreground</p>
        </div>
        <div class="rounded-xl bg-secondary p-4 text-secondary-foreground">
          <p>.bg-secondary + .text-secondary-foreground</p>
        </div>
        <div class="rounded-xl bg-accent p-4 text-accent-foreground">
          <p>.bg-accent + .text-accent-foreground</p>
        </div>
      </div>

      <div class="grid gap-4 text-sm md:grid-cols-2">
        <div class="rounded-2xl bg-card p-4 text-card-foreground">
          <p>.bg-card + .text-card-foreground</p>
        </div>
        <div class="rounded-2xl bg-popover p-4 text-popover-foreground">
          <p>.bg-popover + .text-popover-foreground</p>
        </div>
      </div>
    </section>

    {/* GRADIENTES Y TEXTO GRADIENTE */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">Gradientes y utilidades custom</h2>

      <p class="text-gradient-primary text-2xl font-orbitron">
        .text-gradient-primary → texto con gradiente primario
      </p>
      <p class="text-gradient-secondary text-2xl font-orbitron">
        .text-gradient-secondary → texto con gradiente secundario
      </p>

      <div class="grid gap-4 text-sm md:grid-cols-2">
        <div class="h-24 rounded-2xl bg-gradient-primary p-4">
          <p>.bg-gradient-primary</p>
        </div>
        <div class="h-24 rounded-2xl bg-gradient-secondary p-4">
          <p>.bg-gradient-secondary</p>
        </div>
        <div class="h-24 rounded-2xl bg-gradient-hero p-4">
          <p>.bg-gradient-hero</p>
        </div>
        <div class="h-24 rounded-2xl bg-gradient-card p-4">
          <p>.bg-gradient-card</p>
        </div>
      </div>

      <div
        class="h-24 rounded-2xl bg-gradient-radial from-primary/40 via-transparent to-transparent p-4"
      >
        <p>
          .bg-gradient-radial + from-primary/40 via-transparent to-transparent
        </p>
      </div>
    </section>

    {/* GLOW / SOMBRAS */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">Glow y sombras</h2>
      <div class="grid gap-6 md:grid-cols-3">
        <div class="glow-primary rounded-2xl bg-card p-4 text-sm">
          <p>.glow-primary</p>
        </div>
        <div class="glow-primary-lg rounded-2xl bg-card p-4 text-sm">
          <p>.glow-primary-lg</p>
        </div>
        <div class="glow-secondary rounded-2xl bg-card p-4 text-sm">
          <p>.glow-secondary</p>
        </div>
      </div>

      <div class="border-glow rounded-2xl bg-card p-4 text-sm">
        <p>.border-glow → borde + glow interno</p>
      </div>
    </section>

    {/* GLASS & BG-GRID */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">Glassmorphism & bg-grid</h2>
      <div class="space-y-4 rounded-2xl bg-card/40 p-4 bg-grid">
        <div class="glass-strong rounded-xl p-4 text-sm">
          <p>.glass → bg-card/60 + blur + borde suave</p>
        </div>
        <div class="glass-strong rounded-xl p-4 text-sm">
          <p>.glass-strong → versión más fuerte de glass</p>
        </div>
        <p class="text-xs text-muted-foreground">
          El fondo de esta sección tiene .bg-grid, deberían verse líneas muy
          sutiles tipo rejilla.
        </p>
      </div>
    </section>

    {/* ANIMACIONES */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">Animaciones</h2>
      <p class="text-xs text-muted-foreground">
        Si las animaciones están bien configuradas, deberías ver movimiento en
        los elementos de abajo.
      </p>

      <div class="flex flex-wrap items-center gap-6">
        <div class="flex flex-col items-center gap-2">
          <div class="h-10 w-10 rounded-full bg-primary/60 animate-float"></div>
          <span class="text-xs text-muted-foreground">.animate-float</span>
        </div>

        <div class="flex flex-col items-center gap-2">
          <div
            class="h-10 w-10 rounded-full bg-secondary/60 animate-pulse-glow"
          >
          </div>
          <span class="text-xs text-muted-foreground">.animate-pulse-glow</span>
        </div>

        <div class="flex flex-col items-center gap-2">
          <div class="h-10 w-10 rounded-full bg-accent/60 animate-glow-pulse">
          </div>
          <span class="text-xs text-muted-foreground">.animate-glow-pulse</span>
        </div>

        <div class="flex flex-col items-center gap-2">
          <button
            class="rounded-full bg-gradient-primary px-4 py-2 text-xs text-primary-foreground animate-scale-in"
          >
            .animate-scale-in
          </button>
          <span class="text-xs text-muted-foreground"
            >Botón entra con scale-in</span
          >
        </div>

        <div class="relative h-16 w-40 overflow-hidden rounded-xl bg-card">
          <div
            class="absolute inset-x-0 h-10 w-[200%] bg-gradient-to-b from-primary/70 via-transparent to-transparent animate-[scan-line_3s_linear_infinite]"
          >
          </div>
          <span
            class="relative z-10 flex h-full items-center justify-center text-[11px] text-muted-foreground"
          >
            .animate-[scan-line_3s_linear_infinite]
          </span>
        </div>
      </div>
    </section>

    {/* ANIMATED BORDER */}
    <section class="space-y-4">
      <h2 class="text-xl font-orbitron">animated-border</h2>
      <div class="animated-border relative rounded-2xl bg-card p-4 text-sm">
        <p>
          .animated-border → debería verse un borde con gradiente animado
          alrededor de este bloque.
        </p>
      </div>
    </section>

    {/* SCROLLBAR TEST */}
    <section class="space-y-2">
      <h2 class="text-xl font-orbitron">Scrollbar custom</h2>
      <p class="text-xs text-muted-foreground">
        Si haces scroll en este bloque, deberías ver el scrollbar fino con color
        cian.
      </p>
      <div
        class="max-h-40 overflow-y-auto rounded-xl bg-card p-4 text-xs text-muted-foreground"
      >
        {
          Array.from({ length: 20 }).map((_, i) => (
            <p>Linea {i + 1}: texto de prueba para ver el scrollbar.</p>
          ))
        }
      </div>
    </section>
  </div>
</main>
